---
phase: 03-products
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
autonomous: false
files_modified: []
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06

must_haves:
  truths:
    - "A human has confirmed the products grid renders 4 cards with images, MOQ badge, and custom logo callout"
    - "A human has confirmed clicking a card navigates to the product detail page"
    - "A human has confirmed the detail page shows description, materials, customization options, and CTA"
    - "A human has confirmed clicking the product image opens the lightbox overlay"
    - "A human has confirmed the lightbox dismisses with ESC key and click outside"
    - "A human has confirmed the JSON-LD script tag appears in page source"
    - "A human has confirmed all pages are usable on a 375px mobile viewport"
  artifacts:
    - path: ".planning/phases/03-products/03-04-SUMMARY.md"
      provides: "Checkpoint sign-off and any issues found"
  key_links:
    - from: "human verification"
      to: "phase gate clearance"
      via: "manual browser testing"
      pattern: "approved"
---

<objective>
Human verification checkpoint: confirm the complete product catalog experience works correctly in a real browser before closing Phase 3.

Purpose: Automated tests cannot verify visual layout, keyboard interactions (ESC key), mobile touch events, or SSR-rendered page source. This checkpoint catches any regressions before the phase is marked complete.

Output: Human sign-off (type "approved") or issue report for the executor to fix.
</objective>

<execution_context>
@/home/whenigone/.claude/get-shit-done/workflows/execute-plan.md
@/home/whenigone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-products/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of product catalog</name>
  <what-built>
    Complete product catalog experience:
    - Products grid at /products: 4 cards with full-bleed images, product names, taglines, MOQ 50 Sets badges, and "Custom Logo Available" callouts. Each card links to its detail page.
    - Product detail pages at /products/{id}: 2-column desktop layout (image + lightbox left, product info right). Sections: name/tagline, description, materials, customization options, lead time callout, Request a Quote CTA.
    - Lightbox: clicking the product image opens a full-screen Radix Dialog overlay with the full-size image.
    - JSON-LD: Product schema with priceSpecification "On Request" injected via head() scripts into each detail page.
  </what-built>
  <action>Run `npm run dev` and follow the verification steps in how-to-verify. Check all 6 steps (32 items total).</action>
  <how-to-verify>
    Run the dev server if not already running:
    ```
    npm run dev
    ```

    Step 1 — Products Grid (375px mobile viewport):
    1. Open http://localhost:3000/products
    2. Confirm 4 product cards are visible — no stub text ("Phase 3"), no lorem ipsum
    3. Each card: image loads (picsum placeholder), product name bold, one-line tagline muted, "MOQ 50 Sets" badge visible, "Custom Logo Available" text with icon visible
    4. Resize browser to 375px width — confirm single-column layout, no horizontal scroll
    5. Resize to 768px — confirm 2-column layout
    6. Resize to 1280px — confirm 4-column layout

    Step 2 — Navigation from grid to detail:
    7. Click "Architect Desk Set" card — confirm URL changes to /products/architect-desk-set
    8. Confirm the detail page loads (not a blank page, not "Product not found")
    9. Press browser Back — confirm you return to /products grid
    10. Test 1 other product card link

    Step 3 — Product Detail Page content:
    11. Navigate to /products/architect-desk-set
    12. Confirm h1 heading matches "Architect Desk Set"
    13. Confirm description paragraph is visible (multiple sentences, no placeholder)
    14. Confirm Materials section shows "Aluminium alloy · Anodised finish"
    15. Confirm Customization section shows at least 3 bullet points
    16. Confirm lead time callout box shows MOQ 50 sets, sample and bulk timelines
    17. Confirm "Request a Quote" CTA button is present at bottom of info column
    18. At 375px mobile: image stacks above product info, no horizontal scroll

    Step 4 — Lightbox:
    19. Click the product image (cursor should show zoom-in cursor)
    20. Confirm a full-screen overlay appears with the enlarged image
    21. Confirm an X button is visible in the overlay
    22. Click the X button — confirm overlay closes
    23. Click the image again to open overlay
    24. Press Escape key — confirm overlay closes
    25. Click the image again, then click outside the image area (on the dark backdrop) — confirm overlay closes

    Step 5 — JSON-LD structured data:
    26. In browser, right-click → "View Page Source" (NOT Inspect / DevTools Elements)
    27. Search for "application/ld+json" in the source
    28. Confirm a script tag with type="application/ld+json" is present
    29. Confirm the JSON contains "priceSpecification" and "On Request"
    30. If JSON-LD is absent: note this in your response — it may be a TanStack Start head() scripts rendering issue (see RESEARCH.md open question #1)

    Step 6 — Unknown product slug:
    31. Navigate to http://localhost:3000/products/nonexistent-product
    32. Confirm a "Product not found" message appears (not a blank page, not a crash)
  </how-to-verify>
  <verify>
    <automated>MISSING — this task is a human-verify checkpoint; visual layout, keyboard ESC dismiss, and page-source JSON-LD cannot be verified with automated commands</automated>
  </verify>
  <done>Human typed "approved". All 6 verification steps passed (or JSON-LD issue noted but not blocking).</done>
  <resume-signal>
    Type "approved" if all 32 steps pass.

    If any steps fail, describe the issue (which step, what you saw vs. what was expected). The executor will fix and re-run the checkpoint.

    EXCEPTION: If Step 30 finds JSON-LD absent in page source, type "approved with note: JSON-LD not rendering" — this is a known TanStack Start RC issue that may require a workaround in Phase 5 (SEO plan). Do not block phase completion on this alone.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off received.
</verification>

<success_criteria>
- Human typed "approved" (with or without JSON-LD note)
- Phase 3 all 6 PROD requirements confirmed working in browser
- Any issues found and fixed before sign-off, or documented as known limitations
</success_criteria>

<output>
After sign-off, create `.planning/phases/03-products/03-04-SUMMARY.md` using the summary template. Record the checkpoint outcome (approved, any issues found, JSON-LD status).

Then update ROADMAP.md Phase 3 Plans count and mark all plans with [x].
</output>
