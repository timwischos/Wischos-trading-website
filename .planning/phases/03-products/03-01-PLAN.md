---
phase: 03-products
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/content/__tests__/products.test.ts
  - src/components/__tests__/ProductCard.test.tsx
  - src/components/__tests__/ProductLightbox.test.tsx
  - src/content/products.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06

must_haves:
  truths:
    - "Three test files exist and import from known file paths without syntax errors"
    - "products.ts has 4 product objects with all required fields populated (even if placeholder copy)"
    - "Each product.id is a non-empty kebab-case string with no spaces or special characters"
    - "npx vitest run exits cleanly — no parse errors, even if some tests are marked pending or fail on data assertions"
  artifacts:
    - path: "src/content/__tests__/products.test.ts"
      provides: "Data-shape tests for PROD-01, PROD-02, PROD-03, PROD-06"
      exports: ["products array assertions"]
    - path: "src/components/__tests__/ProductCard.test.tsx"
      provides: "Rendering test for PROD-05 customization callout"
    - path: "src/components/__tests__/ProductLightbox.test.tsx"
      provides: "Dialog open/close behavior test for PROD-04"
    - path: "src/content/products.ts"
      provides: "4 product objects satisfying the Product interface"
  key_links:
    - from: "src/content/__tests__/products.test.ts"
      to: "src/content/products.ts"
      via: "import { products } from '@/content/products'"
      pattern: "import.*products.*from.*@/content/products"
---

<objective>
Create the Wave 0 test scaffold: three test files covering all six PROD requirements, and populate `products.ts` with 4 real product objects so the test imports resolve and data tests pass GREEN immediately.

Purpose: Nyquist rule — every subsequent implementation plan has automated tests waiting for it. Writing data and data tests in the same wave is intentional: the product data is static TypeScript and there is no meaningful RED phase for content shape tests when the data is authored at the same moment. Component tests (ProductCard, ProductLightbox) will go RED now and GREEN when Plans 03-03 and 03-04 create those components.

Output: 3 test files in src/content/__tests__/ and src/components/__tests__/, updated src/content/products.ts with 4 real product objects.
</objective>

<execution_context>
@/home/whenigone/.claude/get-shit-done/workflows/execute-plan.md
@/home/whenigone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-products/03-CONTEXT.md
@.planning/phases/03-products/03-RESEARCH.md
@.planning/phases/03-products/03-VALIDATION.md

<interfaces>
<!-- Existing Product interface in src/content/products.ts — DO NOT CHANGE this interface -->
```typescript
export interface Product {
  id: string           // kebab-case slug, used as URL param
  name: string
  tagline: string
  description: string
  category: string
  materials: string[]
  heroImage: string    // picsum.photos/seed/{id}/800/600
  moq: number          // must be 50
  customizationOptions: string[]   // must have at least 1 item
  leadTimeSample: string
  leadTimeBulk: string
}

export const products: Product[] = []  // currently empty — this plan fills it
```

<!-- Existing test patterns from Phase 2 — follow the same style -->
From src/content/__tests__/homepage.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { homepage } from '@/content/homepage'

describe('homepage content', () => {
  it('hero has required string fields', () => {
    expect(homepage.hero.kicker).toBeTruthy()
    // ...
  })
})
```

From src/content/__tests__/footer.test.tsx (RTL + TanStack Router pattern):
```typescript
import { render, act } from '@testing-library/react'
import { createMemoryHistory, createRootRoute, createRouter, RouterProvider } from '@tanstack/react-router'

function createTestRouter() {
  const rootRoute = createRootRoute({ component: SiteFooter })
  const router = createRouter({
    routeTree: rootRoute,
    history: createMemoryHistory({ initialEntries: ['/'] }),
  })
  return router
}
// await act(async () => { render(<RouterProvider router={router} />); await router.load() })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate products.ts with 4 real product objects</name>
  <files>src/content/products.ts</files>
  <behavior>
    - products array has exactly 4 items
    - Each item: id is non-empty, lowercase kebab-case, no spaces (regex /^[a-z0-9-]+$/)
    - Each item: name, tagline, description, category, heroImage, leadTimeSample, leadTimeBulk are non-empty strings
    - Each item: moq === 50
    - Each item: materials.length >= 1
    - Each item: customizationOptions.length >= 1
    - heroImage URLs use pattern https://picsum.photos/seed/{id}/800/600
  </behavior>
  <action>
    Replace the empty `products` array in src/content/products.ts with 4 product objects. Keep the existing `Product` interface unchanged — only update the `products` array.

    Use these exact 4 products as specified in the phase CONTEXT.md decisions:

    ```typescript
    export const products: Product[] = [
      {
        id: 'architect-desk-set',
        name: 'Architect Desk Set',
        tagline: 'Brake-formed aluminium desk accessories — refined, functional, unmistakably yours.',
        description: 'The Architect Desk Set comprises a coordinated trio of brake-formed aluminium components: an in/out document tray, a vertical document holder, and a catchall dish. Each piece is manufactured from 1.5mm aluminium alloy with a satin anodised finish. Edges are deburred and radiused for a refined tactile experience. The set ships flat-packed inside a branded kraft sleeve. Logo engraving is precision CNC-milled into the catchall dish face.',
        category: 'Desk Accessories',
        materials: ['Aluminium alloy', 'Anodised finish'],
        heroImage: 'https://picsum.photos/seed/architect-desk-set/800/600',
        moq: 50,
        customizationOptions: [
          'CNC logo engraving on catchall dish face',
          'Custom colour anodising (MOQ conditions apply)',
          'Branded kraft sleeve with your logo and colour palette',
        ],
        leadTimeSample: '7–10 business days',
        leadTimeBulk: 'Approximately 30 days',
      },
      {
        id: 'signature-pen-collection',
        name: 'Signature Pen Collection',
        tagline: 'Precision-machined brass writing instruments in a custom gift box.',
        description: 'The Signature Pen Collection is a matched pair of writing instruments — a rollerball and a ballpoint — machined from solid brass rod stock and finished with a brushed surface. The weighted barrel (28g each) sits firmly in hand. Both pens use standard international refills. The collection ships in a hand-stitched presentation box with a debossed lid panel — sized for desk display or client gifting. Logo placement is laser-engraved on the barrel clip.',
        category: 'Writing Instruments',
        materials: ['Brass', 'Stainless steel clip'],
        heroImage: 'https://picsum.photos/seed/signature-pen-collection/800/600',
        moq: 50,
        customizationOptions: [
          'Laser engraving on barrel clip (logo or text)',
          'Debossed logo on presentation box lid',
          'Custom interior lining colour',
        ],
        leadTimeSample: '7–10 business days',
        leadTimeBulk: 'Approximately 30 days',
      },
      {
        id: 'castro-catchall-tray',
        name: 'Castro Catchall Tray',
        tagline: 'Stamped brass desk catchall with custom logo engraving — a daily-use object with presence.',
        description: 'The Castro Catchall Tray is a single-piece stamped brass dish, 180mm × 130mm, with a 12mm upturned rim. The surface takes a light brushed finish that develops a natural patina over time. The floor of the tray is the engraving field: your logo is CNC-etched into the brass, creating a permanent, tactile brand mark. The tray is supplied in a branded tissue-wrapped carton with a printed card insert. No assembly required.',
        category: 'Desk Accessories',
        materials: ['Brass'],
        heroImage: 'https://picsum.photos/seed/castro-catchall-tray/800/600',
        moq: 50,
        customizationOptions: [
          'CNC logo engraving on tray floor',
          'Branded carton with custom print',
          'Custom card insert with brand message',
        ],
        leadTimeSample: '7–10 business days',
        leadTimeBulk: 'Approximately 30 days',
      },
      {
        id: 'executive-edc-set',
        name: 'Executive EDC Set',
        tagline: 'Coordinated brass everyday-carry accessories in branded gift packaging.',
        description: 'The Executive EDC Set is a coordinated trio of everyday-carry items: a split-ring keyring, a twist-gate carabiner clip, and a slim card case — all machined from solid brass. Each piece has consistent proportions and a brushed finish. Together, they make a coherent desk-to-pocket set. The set ships in a rigid lid-and-base gift box with a custom-printed outer. The card case lid carries a laser-engraved logo. Ideal for new-hire gifting, client appreciation, or corporate milestone programmes.',
        category: 'EDC / Travel',
        materials: ['Brass', 'Stainless steel'],
        heroImage: 'https://picsum.photos/seed/executive-edc-set/800/600',
        moq: 50,
        customizationOptions: [
          'Laser engraving on card case lid',
          'Custom-printed outer gift box',
          'Custom card insert with brand message',
        ],
        leadTimeSample: '7–10 business days',
        leadTimeBulk: 'Approximately 30 days',
      },
    ]
    ```
  </action>
  <verify>
    <automated>npx vitest run src/content/__tests__/products.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>src/content/products.ts exports 4 Product objects. The products.test.ts from Task 2 passes green after this task completes (Task 2 must run first to create the test file, but this task can be written first — the executor should complete both tasks before running verify).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffold — products.test.ts, ProductCard.test.tsx, ProductLightbox.test.tsx</name>
  <files>src/content/__tests__/products.test.ts, src/components/__tests__/ProductCard.test.tsx, src/components/__tests__/ProductLightbox.test.tsx</files>
  <behavior>
    products.test.ts assertions (PROD-01, PROD-02, PROD-03, PROD-06):
    - products array has exactly 4 items
    - Each product has a non-empty id matching /^[a-z0-9-]+$/ (kebab-case, URL-safe)
    - Each product has non-empty name, tagline, heroImage strings
    - Each product has moq === 50
    - Each product has materials.length >= 1
    - Each product has customizationOptions.length >= 1
    - Each product has non-empty description, leadTimeSample, leadTimeBulk
    - JSON-LD helper: for each product, a JSON.stringify of the Product schema shape contains '"priceSpecification"' and '"On Request"'

    ProductCard.test.tsx assertions (PROD-05) — initially RED until Plan 03-03:
    - Import ProductCard from '@/components/sections/ProductGridSection' (or wherever plan 03-03 will place it)
    - Render ProductCard with first product from products array
    - Expect "Custom Logo Available" text to be present in rendered output
    - Expect product.name to be in rendered output

    ProductLightbox.test.tsx assertions (PROD-04) — initially RED until Plan 03-04:
    - Import ProductLightbox from '@/components/sections/ProductDetailSection'
    - Render it with a test product
    - Verify the trigger button is present in DOM
    - Fire click on trigger, verify dialog content appears (check for dialog role or image presence)
    - NOTE: ESC key dismiss is not testable in jsdom — keep as a comment in the test
  </behavior>
  <action>
    NOTE: Execute Task 1 (products.ts data) before Task 2 tests so the import resolves. The executor should create products.ts content first, then these test files.

    Create src/content/__tests__/products.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { products } from '@/content/products'

    describe('products content', () => {
      it('has exactly 4 product series', () => {
        expect(products).toHaveLength(4)
      })

      it('each product has a URL-safe kebab-case id', () => {
        for (const product of products) {
          expect(product.id).toBeTruthy()
          expect(product.id).toMatch(/^[a-z0-9-]+$/)
        }
      })

      it('each product has required string fields', () => {
        for (const product of products) {
          expect(product.name).toBeTruthy()
          expect(product.tagline).toBeTruthy()
          expect(product.description).toBeTruthy()
          expect(product.heroImage).toBeTruthy()
          expect(product.leadTimeSample).toBeTruthy()
          expect(product.leadTimeBulk).toBeTruthy()
        }
      })

      it('each product has moq of 50', () => {
        for (const product of products) {
          expect(product.moq).toBe(50)
        }
      })

      it('each product has at least 1 material', () => {
        for (const product of products) {
          expect(product.materials.length).toBeGreaterThanOrEqual(1)
        }
      })

      it('each product has at least 1 customization option', () => {
        for (const product of products) {
          expect(product.customizationOptions.length).toBeGreaterThanOrEqual(1)
        }
      })

      it('each product heroImage uses picsum with product id as seed', () => {
        for (const product of products) {
          expect(product.heroImage).toContain('picsum.photos/seed/')
          expect(product.heroImage).toContain(product.id)
        }
      })

      it('Product JSON-LD shape contains priceSpecification On Request for each product', () => {
        for (const product of products) {
          const jsonLd = {
            '@context': 'https://schema.org',
            '@type': 'Product',
            name: product.name,
            description: product.description,
            brand: { '@type': 'Brand', name: 'Wischos Gift' },
            offers: {
              '@type': 'Offer',
              priceSpecification: {
                '@type': 'PriceSpecification',
                price: 'On Request',
              },
            },
          }
          const serialised = JSON.stringify(jsonLd)
          expect(serialised).toContain('"priceSpecification"')
          expect(serialised).toContain('"On Request"')
        }
      })
    })
    ```

    Create src/components/__tests__/ProductCard.test.tsx:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import {
      createMemoryHistory,
      createRootRoute,
      createRoute,
      createRouter,
      RouterProvider,
      Outlet,
    } from '@tanstack/react-router'
    import { ProductCard } from '@/components/sections/ProductGridSection'
    import { products } from '@/content/products'

    function createTestRouter(component: React.ComponentType) {
      const rootRoute = createRootRoute({ component: Outlet })
      const indexRoute = createRoute({ getParentRoute: () => rootRoute, path: '/', component })
      const routeTree = rootRoute.addChildren([indexRoute])
      return createRouter({
        routeTree,
        history: createMemoryHistory({ initialEntries: ['/'] }),
      })
    }

    describe('ProductCard', () => {
      const product = products[0]!

      it('renders product name', async () => {
        const router = createTestRouter(() => <ProductCard product={product} />)
        render(<RouterProvider router={router} />)
        await router.load()
        expect(screen.getByText(product.name)).toBeTruthy()
      })

      it('renders "Custom Logo Available" callout', async () => {
        const router = createTestRouter(() => <ProductCard product={product} />)
        render(<RouterProvider router={router} />)
        await router.load()
        expect(screen.getByText(/Custom Logo Available/i)).toBeTruthy()
      })

      it('renders MOQ badge', async () => {
        const router = createTestRouter(() => <ProductCard product={product} />)
        render(<RouterProvider router={router} />)
        await router.load()
        expect(screen.getByText(/MOQ 50/i)).toBeTruthy()
      })
    })
    ```

    Create src/components/__tests__/ProductLightbox.test.tsx:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { render, screen, fireEvent, act } from '@testing-library/react'
    import { ProductLightbox } from '@/components/sections/ProductDetailSection'
    import { products } from '@/content/products'

    // NOTE: ESC key dismiss is NOT tested here — Radix Dialog keyboard events
    // are unreliable in jsdom. Verify ESC dismiss manually in browser.

    describe('ProductLightbox', () => {
      const product = products[0]!

      it('renders an image trigger button', () => {
        render(<ProductLightbox product={product} />)
        const trigger = screen.getByRole('button', { name: new RegExp(product.name, 'i') })
        expect(trigger).toBeTruthy()
      })

      it('opens dialog when image trigger is clicked', async () => {
        render(<ProductLightbox product={product} />)
        const trigger = screen.getByRole('button', { name: new RegExp(product.name, 'i') })
        await act(async () => {
          fireEvent.click(trigger)
        })
        // After click, dialog content should be present
        // Radix renders dialog in a portal — query on document.body
        const dialog = document.body.querySelector('[data-slot="dialog-content"]')
        expect(dialog).not.toBeNull()
      })
    })
    ```

    After all 3 files are created, run the test suite. The products.test.ts tests should be GREEN (products data exists). The ProductCard and ProductLightbox tests will be RED with "Cannot find module" errors until Plans 03-03 and 03-04 create those components — this is expected and correct for Wave 0.
  </action>
  <verify>
    <automated>npx vitest run src/content/__tests__/products.test.ts --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>src/content/__tests__/products.test.ts passes all 8 assertions GREEN. src/components/__tests__/ProductCard.test.tsx and ProductLightbox.test.tsx exist and fail with module-not-found errors (not syntax errors) — confirming correct Wave 0 RED state for component tests.</done>
</task>

</tasks>

<verification>
Data tests pass:
```
npx vitest run src/content/__tests__/products.test.ts
```
All 8 describe blocks green. Component test files exist:
```
ls src/components/__tests__/
```
Shows ProductCard.test.tsx and ProductLightbox.test.tsx. TypeScript check on content module:
```
npx tsc --noEmit 2>&1 | grep products
```
No type errors in products.ts.
</verification>

<success_criteria>
- `npx vitest run src/content/__tests__/products.test.ts` exits 0 with 8 tests passing
- `src/components/__tests__/ProductCard.test.tsx` exists and imports from '@/components/sections/ProductGridSection'
- `src/components/__tests__/ProductLightbox.test.tsx` exists and imports from '@/components/sections/ProductDetailSection'
- `products.ts` has 4 items with all Product interface fields populated
- No product.id contains spaces or uppercase letters
</success_criteria>

<output>
After completion, create `.planning/phases/03-products/03-01-SUMMARY.md` using the summary template.
</output>
