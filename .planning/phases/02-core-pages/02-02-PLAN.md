---
phase: 02-core-pages
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/ValuePropSection.tsx
  - src/components/sections/DifferentiatorSection.tsx
  - src/components/sections/CredibilitySection.tsx
  - src/components/sections/ProductPreviewSection.tsx
  - src/components/sections/CtaSection.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06

must_haves:
  truths:
    - "Homepage renders a hero section with headline and 'MOQ 50 sets' visible above the fold"
    - "Value proposition section shows exactly 3 feature cards"
    - "Differentiators section has 4+ comparison claims"
    - "Credibility section shows 4 stat callouts"
    - "Product preview section shows 3 product teaser cards with a 'View All Products' link to /products"
    - "CTA section has a button/link pointing to /inquiry"
    - "No horizontal scroll at 375px, 768px, 1280px"
    - "Homepage does NOT use PageShell wrapper (hero is full-bleed)"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section component — LAND-01"
      exports: ["HeroSection"]
    - path: "src/components/sections/ValuePropSection.tsx"
      provides: "3-column feature card grid — LAND-02"
      exports: ["ValuePropSection"]
    - path: "src/components/sections/DifferentiatorSection.tsx"
      provides: "Comparison claims list — LAND-03"
      exports: ["DifferentiatorSection"]
    - path: "src/components/sections/CredibilitySection.tsx"
      provides: "4-stat credibility callout row — LAND-04"
      exports: ["CredibilitySection"]
    - path: "src/components/sections/ProductPreviewSection.tsx"
      provides: "3-card product preview grid — LAND-06"
      exports: ["ProductPreviewSection"]
    - path: "src/components/sections/CtaSection.tsx"
      provides: "CTA banner with /inquiry button — LAND-05"
      exports: ["CtaSection"]
    - path: "src/routes/index.tsx"
      provides: "HomePage assembling all 6 section components"
  key_links:
    - from: "src/routes/index.tsx"
      to: "six section components"
      via: "named imports from @/components/sections/*"
      pattern: "import.*Section.*from"
    - from: "CtaSection"
      to: "/inquiry route"
      via: "<Link to='/inquiry'> or Button asChild"
      pattern: "to.*inquiry"
    - from: "ProductPreviewSection"
      to: "/products route"
      via: "<Link to='/products'>"
      pattern: "to.*products"
---

<objective>
Build the six homepage section components and wire them into the index route.

Purpose: Homepage is the primary buyer touchpoint. Six named sections implement the full AIDA persuasion arc: hero (Attention), value props (Interest), differentiators (Desire), credibility (Desire), product preview (Interest/Action), CTA (Action).

Output: src/components/sections/ directory with 6 components. src/routes/index.tsx replaced with full composition.
</objective>

<execution_context>
@/home/whenigone/.claude/get-shit-done/workflows/execute-plan.md
@/home/whenigone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- Content contracts from 02-01 — executor uses these directly -->

From src/content/homepage.ts (written in 02-01):
```typescript
export const homepage = {
  hero: {
    kicker: string,       // 'Premium Corporate Gifting'
    headline: string,     // 'Gift Sets That Reflect Your Brand — Not the Factory'
    subheadline: string,
    cta: string,          // 'Request an Inquiry'
  },
  valueProps: {
    kicker: string,
    heading: string,
    items: Array<{ title: string; description: string; icon: string }>, // 3 items, icon is Lucide icon name
  },
  differentiators: {
    kicker: string,
    heading: string,
    intro: string,
    items: Array<{ claim: string; detail: string }>,  // 4 items
  },
  credibility: {
    kicker: string,
    heading: string,
    stats: Array<{ value: string; label: string }>,  // 4 items
  },
  productPreview: {
    kicker: string,
    heading: string,
    viewAllCta: string,   // 'View All Products'
    items: Array<{ name: string; tagline: string }>,  // 3 items
  },
  cta: {
    heading: string,
    body: string,
    buttonLabel: string,  // 'Request an Inquiry'
  },
} as const
```

Custom CSS classes available (from styles.css — use these):
- `.display-title` — Fraunces serif for hero headline
- `.island-shell` — glassmorphism card (border + gradient + backdrop-blur)
- `.feature-card` — elevated tile with hover lift
- `.island-kicker` — uppercase small label above section headings
- `.rise-in` — 700ms entry animation
- `.page-wrap` — `min(1080px, calc(100% - 2rem))` centred container

CSS custom properties: `--sea-ink`, `--lagoon`, `--lagoon-deep`, `--palm`, `--sand`, `--foam`, `--surface`
Use as: `text-[var(--sea-ink)]`, `bg-[var(--lagoon)]`

Installed libraries to use:
- `lucide-react` — icons (Gem, Package, CheckCircle, ArrowRight, Users, Globe)
- `@/components/ui/button` — shadcn Button
- `@/components/ui/card` — shadcn Card, CardContent, CardHeader
- `@tanstack/react-router` — Link

DO NOT use PageShell in index.tsx — breaks full-bleed hero.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero, ValueProp, and Differentiator sections</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/ValuePropSection.tsx, src/components/sections/DifferentiatorSection.tsx</files>
  <action>
    Create src/components/sections/HeroSection.tsx:
    - Import homepage from `@/content/homepage`, Link from `@tanstack/react-router`, Button from `@/components/ui/button`
    - Full-bleed section using `page-wrap` for inner content constraint
    - Layout: `section` with `py-24 md:py-32 text-center rise-in`
    - Kicker: `<p className="island-kicker mb-4">{homepage.hero.kicker}</p>`
    - H1: `display-title` class, `text-4xl md:text-6xl lg:text-7xl font-bold text-[var(--sea-ink)] mb-6`
    - Subheadline: `text-lg md:text-xl text-[var(--sea-ink)] opacity-80 max-w-2xl mx-auto mb-10`
    - CTA: `<Button asChild size="lg"><Link to="/inquiry">{homepage.hero.cta}</Link></Button>`
    - Ensure no fixed heights that could cause overflow on small viewports

    Create src/components/sections/ValuePropSection.tsx:
    - Import homepage, and icon components from lucide-react. The icon field in homepage.valueProps.items is a string name. Use a lookup map:
      ```typescript
      import { Gem, Package, CheckCircle } from 'lucide-react'
      const iconMap: Record<string, React.ComponentType<{ className?: string }>> = { Gem, Package, CheckCircle }
      ```
    - Section: `py-20 bg-[var(--surface)]` with `page-wrap`
    - Kicker + heading above grid
    - Grid: `grid grid-cols-1 md:grid-cols-3 gap-6 mt-12`
    - Each card: `div` with `feature-card island-shell rounded-xl p-6`
    - Icon: `h-6 w-6 text-[var(--lagoon-deep)] mb-4`
    - Title: `font-semibold text-[var(--sea-ink)] mb-2`
    - Description: `text-sm text-[var(--sea-ink)] opacity-70`

    Create src/components/sections/DifferentiatorSection.tsx:
    - Import homepage
    - Section: `py-20` with `page-wrap`
    - Kicker + heading + intro paragraph (max-w-2xl)
    - Items as a 2-column grid on md+: `grid grid-cols-1 md:grid-cols-2 gap-6 mt-10`
    - Each item: `island-shell rounded-xl p-6`
    - Claim: `font-semibold text-[var(--sea-ink)] mb-2` with a checkmark icon from lucide-react
    - Detail: `text-sm text-[var(--sea-ink)] opacity-70`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three component files compile without TypeScript errors. Each imports only from @/content/homepage and installed libraries. No inline copy strings in JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Credibility, ProductPreview, CTA sections + route assembly</name>
  <files>src/components/sections/CredibilitySection.tsx, src/components/sections/ProductPreviewSection.tsx, src/components/sections/CtaSection.tsx, src/routes/index.tsx</files>
  <action>
    Create src/components/sections/CredibilitySection.tsx:
    - Section: `py-20 bg-[var(--surface)]` with `page-wrap`
    - Kicker + heading centred above
    - Stats row: `grid grid-cols-2 md:grid-cols-4 gap-8 mt-12 text-center`
    - Each stat: value as `text-3xl md:text-4xl font-bold text-[var(--lagoon-deep)] display-title`, label as `text-sm text-[var(--sea-ink)] opacity-70 mt-1`

    Create src/components/sections/ProductPreviewSection.tsx:
    - Import homepage, Link, Card/CardContent/CardHeader from shadcn
    - Section: `py-20` with `page-wrap`
    - Kicker + heading
    - Cards grid: `grid grid-cols-1 md:grid-cols-3 gap-6 mt-10`
    - Each card: shadcn `<Card>` with CardHeader (product name as `font-semibold`) and CardContent (tagline as `text-sm text-muted-foreground`)
    - Below grid: centred "View All Products" link — `<Button asChild variant="outline"><Link to="/products">{homepage.productPreview.viewAllCta}</Link></Button>`
    - This is a Phase 2 TEASER — do NOT add product images, pricing, or detail links. That is Phase 3.

    Create src/components/sections/CtaSection.tsx:
    - Section: `py-24 bg-[var(--lagoon)] text-white text-center` with `page-wrap`
    - Heading: `display-title text-3xl md:text-4xl font-bold mb-4` in white
    - Body: `text-lg opacity-90 max-w-2xl mx-auto mb-8`
    - Button: `<Button asChild size="lg" variant="secondary"><Link to="/inquiry">{homepage.cta.buttonLabel}</Link></Button>`

    Replace src/routes/index.tsx entirely:
    ```tsx
    import { createFileRoute } from '@tanstack/react-router'
    import { siteMeta } from '@/content/meta'
    import { HeroSection } from '@/components/sections/HeroSection'
    import { ValuePropSection } from '@/components/sections/ValuePropSection'
    import { DifferentiatorSection } from '@/components/sections/DifferentiatorSection'
    import { CredibilitySection } from '@/components/sections/CredibilitySection'
    import { ProductPreviewSection } from '@/components/sections/ProductPreviewSection'
    import { CtaSection } from '@/components/sections/CtaSection'

    export const Route = createFileRoute('/')({
      head: () => ({
        meta: [
          { title: siteMeta.routes.home.title },
          { name: 'description', content: siteMeta.routes.home.description },
        ],
      }),
      component: HomePage,
    })

    function HomePage() {
      return (
        <>
          <HeroSection />
          <ValuePropSection />
          <DifferentiatorSection />
          <CredibilitySection />
          <ProductPreviewSection />
          <CtaSection />
        </>
      )
    }
    ```
    Note: NO PageShell wrapper — hero must be full-bleed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>TypeScript compiles clean. All vitest tests still pass. src/routes/index.tsx renders 6 section components with no PageShell. Product preview has a Link to /products and CTA has a Link to /inquiry.</done>
</task>

</tasks>

<verification>
TypeScript and tests pass:
```
npx tsc --noEmit && npx vitest run
```
Dev server starts:
```
npm run dev
```
Manual: visit http://localhost:3000 and verify:
- Hero headline is visible above the fold at 375px (no clipping)
- 3 feature cards show in single column at 375px, 3-column at 1280px
- Differentiators grid shows 4 items
- Credibility stats show 4 numbers
- Product preview has 3 cards
- CTA section has teal/lagoon background
- No horizontal scroll at any breakpoint
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (no regressions)
- src/routes/index.tsx does not contain PageShell
- src/routes/index.tsx imports 6 named section components
- ProductPreviewSection renders a Link to /products
- CtaSection renders a Link to /inquiry
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-02-SUMMARY.md` using the summary template.
</output>
