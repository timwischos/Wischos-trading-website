---
phase: 02-core-pages
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/AboutHeroSection.tsx
  - src/components/sections/ExpertiseSection.tsx
  - src/components/sections/WhyUsSection.tsx
  - src/components/sections/TrustSection.tsx
  - src/routes/{-$locale}/about.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05

must_haves:
  truths:
    - "About page presents Wischos Gift as a professional trading company — no single-person language"
    - "Expertise section covers EU, US, and AU markets explicitly"
    - "Why Us section has 3+ differentiation points"
    - "Trust section shows PLACEHOLDER for registration number with a DEV-only yellow banner warning"
    - "No factory names, certification lists, or disintermediation signals anywhere on the page"
    - "Page is responsive at 375px, 768px, 1280px — no horizontal scroll"
  artifacts:
    - path: "src/components/sections/AboutHeroSection.tsx"
      provides: "About page hero — ABOUT-01"
      exports: ["AboutHeroSection"]
    - path: "src/components/sections/ExpertiseSection.tsx"
      provides: "EU/US/AU market expertise — ABOUT-02"
      exports: ["ExpertiseSection"]
    - path: "src/components/sections/WhyUsSection.tsx"
      provides: "Why Wischos Gift differentiation — ABOUT-03"
      exports: ["WhyUsSection"]
    - path: "src/components/sections/TrustSection.tsx"
      provides: "Trust signals with PLACEHOLDER + DEV warning — ABOUT-04"
      exports: ["TrustSection"]
    - path: "src/routes/{-$locale}/about.tsx"
      provides: "AboutPage composing all 4 section components"
  key_links:
    - from: "src/routes/{-$locale}/about.tsx"
      to: "4 About section components"
      via: "named imports from @/components/sections/*"
      pattern: "import.*Section.*from"
    - from: "TrustSection"
      to: "about.trust.registrationNumber"
      via: "import about from @/content/about"
      pattern: "about\\.trust\\.registrationNumber"
---

<objective>
Build the four About page section components and wire them into the about route.

Purpose: The About page establishes professional credibility for EU/US/AU buyers evaluating Wischos Gift. E-E-A-T principles apply throughout. The DEV-mode PLACEHOLDER warning ensures the operator sees and fills in the registration number before launch.

Output: 4 section components under src/components/sections/. src/routes/{-$locale}/about.tsx replaced with full composition.
</objective>

<execution_context>
@/home/whenigone/.claude/get-shit-done/workflows/execute-plan.md
@/home/whenigone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- Content contracts from 02-01 -->

From src/content/about.ts (written in 02-01):
```typescript
export const about = {
  hero: {
    heading: string,    // 'A Trading Partner Built for Corporate Buyers'
    subheading: string,
  },
  expertise: {
    kicker: string,
    heading: string,
    markets: Array<{ region: string; insight: string }>,  // 3 markets: EU, US, AU
  },
  whyUs: {
    kicker: string,
    heading: string,
    points: Array<{ title: string; body: string }>,  // 3 points
  },
  trust: {
    registrationLabel: string,
    registrationNumber: string,  // '[PLACEHOLDER — awaiting company registration details]'
    registrationNote: string,
    email: string,               // 'inquiries@wischosgift.com'
    qualityStatement: string,
  },
  mission: {
    heading: string,
    body: string,
  },
} as const
```

Custom CSS classes and CSS vars — same as 02-02:
- `.display-title`, `.island-shell`, `.feature-card`, `.island-kicker`, `.rise-in`, `.page-wrap`
- CSS vars: `--sea-ink`, `--lagoon`, `--lagoon-deep`, `--surface`

CRITICAL content constraint (ABOUT-05): The About page MUST NOT contain:
- Any factory name or manufacturer name
- Any certification numbers (ISO, CE, etc.) or certification logos
- Any language suggesting a single-person operation
- Any supplier identification that could enable client disintermediation

About page CAN use PageShell — it is a document-style page without full-bleed sections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AboutHero, Expertise, and WhyUs sections</name>
  <files>src/components/sections/AboutHeroSection.tsx, src/components/sections/ExpertiseSection.tsx, src/components/sections/WhyUsSection.tsx</files>
  <action>
    Create src/components/sections/AboutHeroSection.tsx:
    - Import about from `@/content/about`
    - Section: `py-20 text-center rise-in bg-[var(--surface)]` with `page-wrap`
    - H1: `display-title text-4xl md:text-5xl font-bold text-[var(--sea-ink)] mb-6`
    - Subheading: `text-lg md:text-xl text-[var(--sea-ink)] opacity-80 max-w-2xl mx-auto`
    - Below subheading: render about.mission as a highlighted pull-quote — `<blockquote className="island-shell rounded-xl p-6 mt-10 max-w-2xl mx-auto text-left">` with mission heading as `font-semibold mb-2` and body as `text-sm`

    Create src/components/sections/ExpertiseSection.tsx:
    - Import about from `@/content/about`, and Globe icon from lucide-react
    - Section: `py-20` with `page-wrap`
    - Kicker + heading
    - Markets grid: `grid grid-cols-1 md:grid-cols-3 gap-6 mt-10`
    - Each market card: `island-shell feature-card rounded-xl p-6`
    - Globe icon at top: `h-5 w-5 text-[var(--lagoon-deep)] mb-3`
    - Region: `font-semibold text-[var(--sea-ink)] mb-2`
    - Insight: `text-sm text-[var(--sea-ink)] opacity-70`

    Create src/components/sections/WhyUsSection.tsx:
    - Import about from `@/content/about`, CheckCircle from lucide-react
    - Section: `py-20 bg-[var(--surface)]` with `page-wrap`
    - Kicker + heading
    - Points list: `flex flex-col gap-6 mt-10 max-w-3xl mx-auto`
    - Each point: `flex gap-4 items-start island-shell rounded-xl p-6`
    - CheckCircle icon: `flex-shrink-0 h-5 w-5 text-[var(--lagoon-deep)] mt-0.5`
    - Title: `font-semibold text-[var(--sea-ink)] mb-1`
    - Body: `text-sm text-[var(--sea-ink)] opacity-70`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three component files compile. No factory names, certifications, or single-person language in any component. All strings sourced from @/content/about.</done>
</task>

<task type="auto">
  <name>Task 2: TrustSection and about route assembly</name>
  <files>src/components/sections/TrustSection.tsx, src/routes/{-$locale}/about.tsx</files>
  <action>
    Create src/components/sections/TrustSection.tsx:
    - Import about from `@/content/about`
    - Section: `py-20` with `page-wrap`
    - Heading: `text-2xl font-bold text-[var(--sea-ink)] mb-8 text-center`
    - Content: two-column grid on md+: `grid grid-cols-1 md:grid-cols-2 gap-8`

    Left column — contact & quality:
    - `island-shell rounded-xl p-6`
    - Email as `<a href="mailto:{about.trust.email}">` styled as `text-[var(--lagoon-deep)] font-medium hover:underline`
    - Quality statement as `text-sm text-[var(--sea-ink)] opacity-70 mt-4`

    Right column — registration:
    - `island-shell rounded-xl p-6`
    - Registration label: `text-xs island-kicker mb-2`
    - DEV-only yellow warning banner (CRITICAL — do not omit):
      ```tsx
      {import.meta.env.DEV && about.trust.registrationNumber.includes('PLACEHOLDER') && (
        <p className="bg-yellow-100 text-yellow-800 text-xs px-3 py-2 rounded mb-3 border border-yellow-200">
          DEV: Registration number placeholder — operator must fill in before launch
        </p>
      )}
      ```
    - Registration number text: `text-sm text-[var(--sea-ink)]`
    - Registration note: `text-xs text-[var(--sea-ink)] opacity-60 mt-1`

    Replace src/routes/{-$locale}/about.tsx entirely:
    ```tsx
    import { createFileRoute } from '@tanstack/react-router'
    import { siteMeta } from '@/content/meta'
    import { AboutHeroSection } from '@/components/sections/AboutHeroSection'
    import { ExpertiseSection } from '@/components/sections/ExpertiseSection'
    import { WhyUsSection } from '@/components/sections/WhyUsSection'
    import { TrustSection } from '@/components/sections/TrustSection'

    export const Route = createFileRoute('/{-$locale}/about')({
      head: () => ({
        meta: [
          { title: siteMeta.routes.about.title },
          { name: 'description', content: siteMeta.routes.about.description },
        ],
      }),
      component: AboutPage,
    })

    function AboutPage() {
      return (
        <>
          <AboutHeroSection />
          <ExpertiseSection />
          <WhyUsSection />
          <TrustSection />
        </>
      )
    }
    ```
    Note: No PageShell wrapper — sections manage their own padding and bg alternation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>TypeScript compiles. All vitest tests pass. TrustSection contains import.meta.env.DEV conditional with PLACEHOLDER check. about.tsx imports 4 section components and has no PageShell. No factory names anywhere.</done>
</task>

</tasks>

<verification>
TypeScript and tests:
```
npx tsc --noEmit && npx vitest run
```
Manual: visit http://localhost:3000/about and verify:
- 'Trading Partner' or equivalent professional language in hero
- Three market cards (EU, US, AU) visible in Expertise section
- Three differentiation points in Why Us section
- Yellow DEV warning banner visible in Trust section (in dev mode)
- No mention of factory names, ISO certifications, or single-person language anywhere
- No horizontal scroll at 375px
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (no regressions from about.test.ts)
- src/routes/{-$locale}/about.tsx does not import PageShell
- TrustSection.tsx contains `import.meta.env.DEV` conditional
- grep confirms no factory/certification language: `grep -i "factory\|iso\|certified\|manufacturer" src/components/sections/About*.tsx` returns no results
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-03-SUMMARY.md` using the summary template.
</output>
