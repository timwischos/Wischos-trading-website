---
phase: 01-foundation
plan: 03
subsystem: layout
tags: [tanstack-router, layout, i18n, routing, shadcn-ui]

# Dependency graph
requires:
  - phase: 01-01
    provides: "TanStack Start scaffold, shadcn/ui components, src/content/ modules"
provides:
  - "src/components/layout/SiteHeader.tsx — sticky header with desktop nav + mobile Sheet"
  - "src/components/layout/SiteFooter.tsx — footer with Privacy Policy and contact email"
  - "src/components/layout/PageShell.tsx — consistent content width wrapper"
  - "src/routes/__root.tsx — root layout with SiteHeader/SiteFooter in shellComponent"
  - "src/routes/{-$locale}.tsx — optional locale prefix, enables /page and /en/page"
  - "All page route stubs: /, /about, /contact, /inquiry, /how-it-works, /products, /privacy"
affects: [phase-02, phase-03, phase-04, all-phases]

# Tech tracking
tech-stack:
  added: []  # All packages already installed in Plan 01
  patterns:
    - "Path alias: @/* maps to src/* (not ~/ as in plan template)"
    - "Root layout: shellComponent pattern used (not component) — receives children prop"
    - "i18n routing: {-$locale} optional segment syntax confirmed working in TanStack Router 1.166.7"
    - "Navigation: all labels from src/content/navigation.ts — no hard-coded strings in JSX"
    - "Mobile nav: shadcn Sheet controlled via useState (open/onOpenChange)"
    - "SiteFooter: uses <a href> for privacy link (not Link) to avoid typed-route constraint before privacy route existed"

key-files:
  created:
    - "src/components/layout/SiteHeader.tsx"
    - "src/components/layout/SiteFooter.tsx"
    - "src/components/layout/PageShell.tsx"
    - "src/routes/{-$locale}.tsx"
    - "src/routes/{-$locale}/about.tsx"
    - "src/routes/{-$locale}/contact.tsx"
    - "src/routes/{-$locale}/inquiry.tsx"
    - "src/routes/{-$locale}/how-it-works.tsx"
    - "src/routes/{-$locale}/products/index.tsx"
    - "src/routes/privacy.tsx"
  modified:
    - "src/routes/__root.tsx — replaced template Header/Footer with SiteHeader/SiteFooter"
    - "src/routes/index.tsx — replaced template content with PageShell stub"
    - "src/routeTree.gen.ts — regenerated by build with all new routes"
  deleted:
    - "src/routes/about.tsx — removed template route, replaced by /{-$locale}/about.tsx"

# Self-Check
status: passed
verified:
  - "npm run build exits 0 — no TypeScript errors, no missing imports (43s)"
  - "{-$locale} optional segment syntax confirmed working in installed version"
  - "Both /about and /en/about route stubs created and build passes"
  - "SiteHeader imports navigation from @/content/navigation — no hard-coded strings"
  - "All route stubs have head() with title+description from src/content/meta.ts"
  - "routeTree.gen.ts regenerated with all 8 routes"

deviations:
  - "Path alias is @/* not ~/* — all imports use @/ prefix"
  - "SiteFooter uses <a href='/privacy'> instead of Link to avoid TypeScript error before privacy route was registered in routeTree"
  - "shellComponent pattern retained from scaffold (not switched to component) — both work, shellComponent is TanStack Start's SSR pattern"
  - "Drizzle-kit push to Supabase not yet completed — requires DATABASE_URL env var (handled separately)"
